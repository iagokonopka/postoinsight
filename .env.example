# Banco de dados (compartilhado por api e packages/db)
DATABASE_URL=postgresql://postoinsight:postoinsight@localhost:5432/postoinsight

# API
API_PORT=3000
API_SECRET=troque-em-producao

# Agente (no servidor do cliente — não vai para o .env raiz)
# POSTOINSIGHT_API_URL=wss://api.postoinsight.com.br
# AGENT_TOKEN=<gerado no seed por conector>
# DB_CONNECTION_STRING=Server=HOST;Database=DB;User Id=USER;Password=PASS;
