{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "pnpm --filter @postoinsight/db build && pnpm --filter api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter api start"
  }
}
