POSTOINSIGHT_API_URL=wss://api.postoinsight.com.br
AGENT_TOKEN=<UUID gerado pelo seed — um por posto>
DB_HOST=<IP ou hostname do SQL Server>
DB_PORT=1433
DB_NAME=<nome do banco>
DB_USER=<usuário read-only>
DB_PASSWORD=<senha>
