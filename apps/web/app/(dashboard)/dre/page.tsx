import { PlaceholderPage } from '@/components/PlaceholderPage'

export const metadata = { title: 'DRE Mensal — PostoInsight' }

export default function DrePage() {
  return (
    <PlaceholderPage
      title="DRE Mensal"
      description="Em construção (Task #5). Aguardando endpoint /api/v1/dre."
    />
  )
}
