import { PlaceholderPage } from '@/components/PlaceholderPage'

export const metadata = { title: 'Sincronização — PostoInsight' }

export default function SyncPage() {
  return (
    <PlaceholderPage
      title="Sincronização"
      description="Status de conectores e histórico de syncs será implementado em fase posterior."
    />
  )
}
